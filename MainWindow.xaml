<Window x:Class="SilentInstaller.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Silent Installer" Height="500" Width="800" Background="White">

    <Window.Resources>

        <!-- Button Template for No MouseOver Effect -->
        <ControlTemplate x:Key="NoMouseOverButtonTemplate"   
                 TargetType="Button">
            <Border Background="{TemplateBinding Background}"  
          BorderBrush="{TemplateBinding BorderBrush}"  
          BorderThickness="{TemplateBinding BorderThickness}">
                <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"  
                      VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
            </Border>
            <ControlTemplate.Triggers>
                <Trigger Property="IsEnabled"  
             Value="False">
                    <Setter Property="Background"  
              Value="{x:Static SystemColors.ControlLightBrush}" />
                    <Setter Property="Foreground"  
              Value="{x:Static SystemColors.GrayTextBrush}" />
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>
    </Window.Resources>

    <Grid>

        <Border CornerRadius="5" Background="White" BorderBrush="Gray" BorderThickness="0.3"></Border>

        <!-- Page 1: Selection Screen -->
        <Grid Name="SelectionPage">

            <!--Grid Layout-->
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="3*"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>

            <!-- Background Image -->
            <Grid.Background>
                <ImageBrush Stretch="UniformToFill" AlignmentX="Center" AlignmentY="Center" ImageSource="data/background.png"/>
            </Grid.Background>

            <!-- Overlay -->
            <Rectangle Stroke="Black" Fill="#8A000000" Grid.ColumnSpan="2" Grid.RowSpan="4"/>

            <!-- Logo & Title -->
            <Image Source="data/fullers-logo.png" Height="80" VerticalAlignment="Center" Grid.ColumnSpan="2" Grid.Row="0"/>
            <TextBlock Text="Silent Installer" Foreground="White" TextAlignment="Center" FontSize="24" FontWeight="DemiBold" Grid.Row="1"
                       HorizontalAlignment="Center" VerticalAlignment="Center" Grid.ColumnSpan="2"/>

            <!-- Carousel Container -->
            <Grid Grid.Column="0" Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                    <RowDefinition/>

                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <TextBlock Text="Select Setup Type:" Foreground="White" FontSize="24" FontWeight="DemiBold" Grid.Row="0" Grid.ColumnSpan="3" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button Content="◀" Width="40" Height="40" Grid.Row="1" Grid.Column="0"
                    HorizontalAlignment="Left" VerticalAlignment="Center" Background="Transparent" Foreground="White" FontSize="24" BorderThickness="0"/>
                <!-- Display Area -->
                <Border BorderBrush="Black" BorderThickness="2"  Grid.Column="1" Grid.Row="1">
                    <StackPanel HorizontalAlignment="Center">
                        <Image Name="CategoryImage" Width="100" Height="100" Margin="0,10,0,10"/>
                        <TextBlock Name="CategoryTitle" FontSize="18" FontWeight="Bold"
                               HorizontalAlignment="Center"/>
                        <TextBlock Name="CategoryDescription" FontSize="14"
                               TextAlignment="Center" Margin="10"/>
                    </StackPanel>
                </Border>
                <Button Content="▶" Width="40" Height="40" Grid.Row="1" Grid.Column="2"
    HorizontalAlignment="Right" VerticalAlignment="Center" Background="Transparent" Foreground="White" FontSize="24" BorderThickness="0"/>
                <TextBlock Name="TextDescription" Text="Dummy" Grid.Row="2" Grid.ColumnSpan="3" VerticalAlignment="Center" HorizontalAlignment="Center" FontSize="18" Foreground="White"></TextBlock>
            </Grid>

            <!-- Included Apps -->

            <!-- Start Button -->
            <Button Width="100" Height="40" Grid.Row="3" Grid.ColumnSpan="2"
        Template="{StaticResource NoMouseOverButtonTemplate}"
        FontSize="16" FontWeight="Bold" RenderTransformOrigin="0.5,0.5"
        VerticalAlignment="Center" HorizontalAlignment="Center"
        BorderBrush="White" Background="#00DDDDDD" Foreground="White">

                <Button.Content>
                    <TextBlock Text="Start"/>
                </Button.Content>

                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="5"/>
                    </Style>

                    <Style TargetType="Button">
                        <Setter Property="RenderTransform">
                            <Setter.Value>
                                <ScaleTransform ScaleX="1" ScaleY="1"/>
                            </Setter.Value>
                        </Setter>

                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Trigger.EnterActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation 
                                    Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)" 
                                    To="White" Duration="0:0:0.15"/>
                                            <ColorAnimation 
                                    Storyboard.TargetProperty="(Button.Foreground).(SolidColorBrush.Color)" 
                                    To="Gray" Duration="0:0:0.15"/>
                                            <DoubleAnimation 
                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                                    To="1.2" Duration="0:0:0.15"/>
                                            <DoubleAnimation 
                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                    To="1.2" Duration="0:0:0.15"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.EnterActions>

                                <Trigger.ExitActions>
                                    <BeginStoryboard>
                                        <Storyboard>
                                            <ColorAnimation 
                                    Storyboard.TargetProperty="(Button.Background).(SolidColorBrush.Color)" 
                                    To="#00DDDDDD" Duration="0:0:0.3"/>
                                            <ColorAnimation 
                                    Storyboard.TargetProperty="(Button.Foreground).(SolidColorBrush.Color)" 
                                    To="White" Duration="0:0:0.3"/>
                                            <DoubleAnimation 
                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleX)" 
                                    To="1" Duration="0:0:0.3"/>
                                            <DoubleAnimation 
                                    Storyboard.TargetProperty="(UIElement.RenderTransform).(ScaleTransform.ScaleY)" 
                                    To="1" Duration="0:0:0.3"/>
                                        </Storyboard>
                                    </BeginStoryboard>
                                </Trigger.ExitActions>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Resources>

                <Button.RenderTransform>
                    <ScaleTransform ScaleX="1" ScaleY="1"/>
                </Button.RenderTransform>
            </Button>

        </Grid>

        <!-- Page 2: Installation Progress -->
        <Grid Name="InstallationPage" Visibility="Hidden" Grid.ColumnSpan="3">
            <Image Source="data/fullers-logo.png" Height="80" VerticalAlignment="Top" Margin="0,20,0,0"/>
            <TextBlock Text="Silent Installer" FontSize="24" FontWeight="Bold" 
                       HorizontalAlignment="Center" Margin="0,100,0,0"/>

            <!-- Progress Bar -->
            <ProgressBar Name="InstallProgress" Width="500" Height="20" 
                         HorizontalAlignment="Center" Margin="0,150,0,0"/>

            <!-- Status Message -->
            <TextBlock Text="Custom Message saying state of installation..." 
                       FontSize="14" HorizontalAlignment="Center" Margin="0,180,0,0"/>

            <!-- Log Output -->
            <TextBox Name="OutputLog" Width="500" Height="150" 
                     HorizontalAlignment="Center" Margin="0,220,0,0"/>

            <!-- Abort Button -->
            <Button Content="Abort" Width="100" Height="40" Background="Gold" 
                    FontSize="16" FontWeight="Bold"
                    HorizontalAlignment="Center" Margin="0,400,0,0"/>
        </Grid>
    </Grid>
</Window>
